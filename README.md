## 🌦️ GDG McGill — Introduction to React Workshop

**Project:** Weather App  
**Tech Stack:** React.js • CSS • OpenWeather API

---

### 📘 Workshop Overview

Welcome to the **Google Developer Group (GDG) McGill** hands-on workshop on **React.js**!  
In this session, we learned the fundamentals of building a dynamic weather app using **React**, styled with **classic CSS**, and powered by live data from the **OpenWeather API**.

By the end of this workshop, participants gained practical experience in:

- Creating and organizing a React project with **Vite**
- Using **React Hooks** such as `useState`, `useEffect`, and `useRef`
- Fetching and displaying API data dynamically
- Structuring reusable components and applying modular CSS

---

### 🧠 Core React Concepts Covered

- **Components** — The building blocks of any React app
- **Props and State** — Passing and managing dynamic data
- **Hooks** — `useState`, `useEffect`, `useRef` for managing reactivity
- **Conditional Rendering** — Displaying content based on fetched data
- **API Integration** — Fetching and parsing JSON from external APIs
- **Classic CSS Styling** — Applying structure and style to components

---

### ⚙️ Installation & Setup Guide

Follow these steps to run the **GDG McGill React Weather App** on your local machine.

---

### 🧩 Prerequisites

Before starting, make sure you have the following installed:

- [Node.js](https://nodejs.org/) (v18 or higher)
- [npm](https://www.npmjs.com/) (comes with Node.js)
- A code editor like [VS Code](https://code.visualstudio.com/)
- An **OpenWeather API key** → [Get one here](https://openweathermap.org/api)

---

### Step 1 — Clone the Repository

```bash
git clone https://github.com/dwei-exe/react-workshop-gdg.git
cd react-weather-app
```

### Step 2 — Install Dependencies

Run the following command to install all required packages:

```bash
npm install
```

### Step 3 - Start the app locally with:

```bash
npm run dev
```

🌤️ How It Works

The user enters a city name into the search bar.

The app fetches weather data from the OpenWeather API.

Key information (temperature, weather condition, icons) is displayed on screen.

React hooks update the UI automatically when the data changes.

🚀 Next Steps: Practice on Your Own

Want to level up your skills? Here are some challenges to try:

Add More Weather Data
Display extra fields like humidity, wind speed, and pressure from the JSON response.

Handle Multiple Cities
Add a feature that lets users add and delete multiple cities to view their weather data side by side.

Explore Tailwind CSS
Now that you know traditional CSS, try styling your components using Tailwind CSS:
👉 https://tailwindcss.com/

💡 Final Thoughts

React’s reactivity and modular architecture make it one of the most in-demand front-end frameworks today.
We hope this workshop helped you understand why — and gave you a foundation to start building your own interactive web apps!

Have fun coding and keep exploring!
— Google Developer Group @ McGill 💙
